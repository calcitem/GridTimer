# .gitignore 说明文档

## 已过滤的重要文件类型

### 🔒 锁定和元数据文件
- **pubspec.lock** - Flutter 依赖锁定文件（推荐不提交，除非是应用项目需要精确版本控制）
- **.metadata** - Flutter 生成的项目元数据

### 🏗️ 构建产物
- **build/** - Flutter 构建输出目录
- **android/app/debug, profile, release** - Android 构建产物
- **android/.gradle** - Gradle 缓存
- **android/local.properties** - 本地 SDK 路径配置（因人而异）
- **android/key.properties** - 签名密钥配置（敏感信息）

### 🔧 生成的代码文件
这些文件会在运行 `./tool/flutter-init.sh` 或 `dart run build_runner build` 时自动生成：

- **\*.g.dart** - JSON 序列化、Hive TypeAdapter 生成文件
- **\*.freezed.dart** - Freezed 生成的不可变类和 copyWith 方法
- **lib/generated/** - Flutter 自动生成的文件
- **lib/l10n/generated/** - 国际化生成的代码

### 📝 IDE 配置文件
- **.idea/** - IntelliJ IDEA / Android Studio 配置
- **.vscode/** - VS Code 配置
- **\*.iml, \*.ipr, \*.iws** - IntelliJ 项目文件

### 🔐 敏感配置
- **android/key.properties** - Android 签名密钥配置
- **android/local.properties** - 本地 Android SDK 路径
- **ios/Pods/** - iOS CocoaPods 依赖（如果有的话）

## ✅ 应该提交的文件

### 源代码
- `lib/**/*.dart` - Dart 源文件（排除 *.g.dart 和 *.freezed.dart）
- `android/app/src/main/**` - Android 原生代码
- `ios/**` - iOS 原生代码（如果实现的话）

### 配置文件
- `pubspec.yaml` - Flutter 依赖配置
- `analysis_options.yaml` - Dart 分析器配置
- `l10n.yaml` - 国际化配置
- `android/app/build.gradle` - Android 构建配置
- `android/build.gradle` - Android 项目配置
- `android/settings.gradle` - Android 设置
- `android/gradle.properties` - Gradle 属性

### 资源文件
- `assets/**` - 应用资源（图片、音频等）
- `android/app/src/main/res/**` - Android 资源文件
- `lib/l10n/*.arb` - 国际化翻译文件（ARB 格式）

### 文档和脚本
- `README.md`, `SETUP.md`, etc. - 项目文档
- `tool/**/*.sh` - 构建和初始化脚本
- `LICENSE` - 开源许可证

### 模板文件
- `android/local.properties.template` - 本地配置模板（实际的 local.properties 不提交）

## 📋 首次克隆后需要做什么

1. **生成本地配置**
   ```bash
   # 如果不存在，Flutter 工具会自动创建 android/local.properties
   flutter doctor
   ```

2. **安装依赖**
   ```bash
   flutter pub get
   ```

3. **生成代码**
   ```bash
   ./tool/flutter-init.sh
   # 或者
   dart run build_runner build --delete-conflicting-outputs
   flutter gen-l10n
   ```

4. **添加资源文件**
   - 将音频文件放到 `assets/sounds/` 和 `android/app/src/main/res/raw/`
   - 生成应用图标放到 `android/app/src/main/res/mipmap-*/`

## 🎯 为什么这样设置

### pubspec.lock 不提交的原因
- **应用项目**：通常不提交（让每个环境获取最新兼容版本）
- **库项目**：应该提交（确保可重现的构建）
- GridTimer 是应用项目，所以不提交

### 生成文件不提交的原因
- 可以通过源文件完全重新生成
- 避免合并冲突
- 保持仓库干净
- 代码评审更专注于源代码

### .metadata 不提交的原因
- Flutter 工具生成的本地元数据
- 不影响项目构建
- 每个开发者的可能不同

## ⚠️ 注意事项

1. **首次克隆项目会缺少生成文件** - 这是正常的！运行 `./tool/flutter-init.sh` 即可
2. **不要手动编辑 .g.dart 和 .freezed.dart** - 它们会被覆盖
3. **签名配置永远不要提交** - 使用 CI/CD 环境变量或安全存储
4. **音频文件需要手动添加** - 因为许可证原因未包含在仓库中

## 📚 相关文档

- Flutter 官方 .gitignore 建议: https://flutter.dev/docs/get-started/install
- build_runner 文档: https://pub.dev/packages/build_runner
- Freezed 文档: https://pub.dev/packages/freezed


