<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Android 13+ runtime permission (still request at runtime) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Needed so scheduled notifications can be rescheduled after reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- Exact alarms: user-granted special access on Android 14+ -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>

    <!-- Full-screen intent: special app access on Android 14+ -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>

    <!-- Optional but common for alarm UX -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- Needed to temporarily adjust system alarm stream volume for reliable ringing -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>

    <!-- Internet for TTS engine downloads (optional) -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Temporarily bypass keyguard when alarm rings (no user prompt needed) -->
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD"/>

    <!-- Keep screen on during alarm (no user prompt needed) -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- Queries for email client (Android 11+) - Required for Catcher email error reporting -->
    <!-- Queries for TTS engines (Android 11+) - Required for flutter_tts to detect TTS engines -->
    <!-- Queries for URL launcher (Android 11+) - Required for url_launcher to open web links -->
    <queries>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        <!-- TTS Engine query - required for flutter_tts on Android 11+ -->
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
        <!-- URL launcher queries - required for url_launcher on Android 11+ -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
    </queries>

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:showWhenLocked="true"
            android:turnScreenOn="true">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Required receivers for flutter_local_notifications -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />

        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false" />

        <!-- Boost/restore Android alarm stream volume at ringing time -->
        <receiver
            android:name=".AlarmVolumeBoostReceiver"
            android:exported="false" />

        <!-- Grid Timer home screen widget -->
        <receiver
            android:name=".GridTimerWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
